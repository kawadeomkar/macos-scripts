# Local Claude Code context — not for public repo
CLAUDE.md

# macOS
.DS_Store

# Logs written by brew-app-scan.sh
*.log
*.err

# Secrets / environment files (never commit these)
.env
.env.*
*.pem
*.key
credentials*
secrets*
